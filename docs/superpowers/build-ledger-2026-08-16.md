# SDD ledger — plan: docs/superpowers/plans/2026-08-15-budget-tracker.md

Spec: docs/superpowers/specs/2026-08-15-budget-tracker-design.md (v1.3, binding authority)
Session: 2026-08-15 overnight autonomous run. User asleep; full build authorized ("finish it before i wake up").
Git HEAD at start: bc40dd2 (only commit; everything since uncommitted).

## Rulings (pre-execution)

- Ruling: NO git worktree; build in place on master — user explicitly instructed to build here and paused all git commits; worktree machinery is meaningless without commits. Cost if wrong: dirty main tree, but tree was empty of code before this run.
- Ruling: GIT COMMITS PAUSED (user instruction, see memory no-git-commits). Implementers NEVER run git commit. Per-task diffs use commit-free plumbing: snapshot = `git add -A && git write-tree` → tree hash; review package = `git diff <baseTree> <headTree>`. Tree objects create no history and nothing to push; judged compliant with "dont commit anything to git for now". Ledger records tree hashes instead of commit hashes. Cost if wrong: user objects to object-db writes — recoverable, `git gc` cleans unreachable trees.
- Ruling: Tasks 20 (sharing packs, spec §11) and 21 (installers, spec §9) appended to plan by Opus agent after Task 19 was written; execution order unchanged (20 after 14 deps exist; 21 last). Cost if wrong: plan formatting drift — caught by my format check before dispatch.
- Ruling: plan's two additions beyond spec accepted and spec updated to match: qrcode@^1 dependency (offline TOTP QR), totp_recovery_codes table (§3 now lists it). Cost if wrong: none, spec is synced.
- Ruling: review queue excludes is_transfer rows (plan Task 12 refinement of spec §4) — accepted; transfers are excluded from all reporting per §3, forcing them through categorization review would be noise. Cost if wrong: transfer rows lack categories; harmless since excluded from reports.
- Ruling: model map (explicit on every dispatch) — implementers: sonnet for all tasks (plan contains complete code; transcription+testing), haiku considered for T3 but sonnet kept for turn-count reliability. Task reviewers: opus for load-bearing correctness tasks (2 schema, 4 auth, 6 TOTP, 9 dedup, 10 undo, 12 engine, 15 budgets), sonnet for the rest. Fix-loop rounds 4-5: escalate implementer to opus (or fable if opus was the stuck model). Final whole-branch review: opus. Cost if wrong: review quality gap — mitigated by final review.
- Ruling: npm install / create-next-app network use permitted (build-time only; spec forbids runtime network calls, not build-time). OneDrive folder will sync node_modules — slow but functional; not relocating project without user.

## Preflight conflict scan (plan v. itself and Global Constraints)

Checked via targeted greps + samples over the 17,320-line plan (full read not performed; sampling documented):

| Seam / pair | Produces vs consumes | Found |
|---|---|---|
| T2 schema → all later tasks | Drizzle table exports, createTestDb()/setDbForTests() helpers | Convention block mandates helper; T2 produces it; spot-checked consumers reference same names. OK |
| T3 money/dates → T15/16/17 | cents + month helpers | Declared in conventions; not re-verified per-signature (grep sampled resolveBudget only). Risk: low. |
| T4 sessions → T5/6/7 | createSession(userId, meta?) / requireAdmin() | Interfaces block (line 2561/2571) matches implementation (3098/3234). OK |
| T8 parseCsv → T13 | parseCsv(buf, mapping): ParseResult | Declaration 5805 = implementation 6340. OK |
| T9 dedupHash → T10 | dedupHash(input{...}) | Declaration 6741 = implementation 7005; golden-vector + invariance tests present (6821-6839). OK |
| T10 hooks.ts → T11/T12 | safe-default hooks replaced by full rewrites | Plan explicitly stages this (agent-reported; T10 note at line 7157 confirms commitImport does NOT run engine; T13 calls runEngine after). OK |
| T12 classify → T14 review queue | classify(tokens): {categoryId, margin} | null | Declaration 8260 = implementation 8880. OK |
| T15 resolveBudget → T17 dashboard | resolveBudget(scope, userId, categoryId, month) | Declaration 13184 = implementation 13517. OK |
| Placeholder scan | TBD/TODO/similar-to/add-error-handling | 0 hits. OK |
| Spec-keyword coverage | transaction_imports, hash_version, attributed_user_id, VACUUM INTO, windows-1252, COALESCE(user_id,0), PAYMENT - THANK YOU, TRUST_PROXY, images.unoptimized, bookworm-slim, totp-v1, Sec-Fetch, log-margin | 148 hits total, all families present. OK |
| Task self-consistency | per-task test-vs-code agreement | Sampled T9 fully (consistent); relying on per-task reviewers for the rest. |

Scan result: no conflicts requiring pre-execution rulings beyond those above.

- Ruling: plan Tasks 20-21 appended (lines 17301-21153; verified Tasks 1-19 untouched by appender). Three literal-spec deviations accepted: (1) rules-pack JSON gains optional `rule_kind` + `category_parent` fields (backward compatible, spec example still imports — test proves it); (2) scripts/reset-admin-password.ts uses better-sqlite3+argon2 directly instead of app modules (standalone image ships compiled chunks, not src/; drift-pin test locks argon2 params to Task 4's verifyPassword); (3) PowerShell installer tests are parser+flag-parity only, skipped when pwsh absent. Cost if wrong: minor doc drift — spec updated to match (1).

- Ruling: user approved 3 mid-flight design changes before any code built (2026-08-15 late evening): (1) manual-only update tooling w/ health-check rollback, no auto-update/scheduler/banner; (2) SimpleFIN connector, dormant, manual sync, external_id dedup, CSV-vs-SimpleFIN account exclusivity; (3) display_description + display_source + merchant rename rules (manual-wins). Spec → v1.4 (partly by me, completed by plan agent); plan amendments (T2/T12/T14/T21 + new T22) delegated to Opus plan agent. Briefs 2/12/14/21/22 must be re-extracted after amendment. First T1 implementer killed by user interrupt pre-write (tree verified clean); relaunched.
- Ruling (user reminder): Fable = planner/decision-maker ONLY; all mechanical work incl. doc edits delegated to Opus/Sonnet.

## Task progress

(entries appended below as tasks complete)
- Task 1: dispatched (implementer sonnet, second launch after user-interrupt kill of first). BASE tree 78e6305. Plan totals 22 tasks once T22 lands.
- Plan amendment complete: spec v1.4 (371 lines), plan 24,484 lines / 22 tasks / 333 steps. Agent's 5 conflict resolutions all accepted (ruling). Watch-items for reviewers: T20 pack-import behavior on packs containing rename rules (skip-vs-reject UX), T22's 5 documented edits to T10 commitImport (CSV path must take original branch). All 21 briefs regenerated post-amendment.
- Task 1: review APPROVED (tree 78e6305..beda2bd, package review-78e6305..beda2bd.diff). Important finding adjudicated — Ruling: "undisclosed docs changes" in diff = concurrent plan-agent v1.4 edits sharing snapshot window, NOT implementer misconduct (I dispatched both; ground truth known). Process fix: fresh BASE immediately before each dispatch; no doc edits during implementer runs. Cost if wrong: none — cause verified. Minor findings (user requested fix now): *.tsbuildinfo gitignore, engines field, duplicate .next/ pattern → fix round 1 dispatched to original implementer. Report annotation requested.
- Ruling: user lifted commit pause 2026-08-16 — commit per completed task, personal identity (pending user paste), private GitHub remote (pending URL), push only on explicit instruction. T1 commit happens once identity is set.
- Ruling: LICENSE = MIT recommended to user (their call pending); README already covered by Task 19 (spec §9) — T19 dispatch will carry explicit "README opens with what-this-app-is/does feature overview + LICENSE file" instruction.
- Task 1: fix round 1/5 (3 addressed, 0 open — tsbuildinfo gitignore, engines field, report annotation; trees beda2bd..e3cd9eb)
- Task 1: complete (trees 78e6305..e3cd9eb, review clean after fix round 1). COMMIT PENDING user identity — batch-commit T1 once identity arrives.
- Task 2: dispatched (implementer sonnet). BASE tree e3cd9eb.
- Git setup COMPLETE (2026-08-16): repo-local identity manavgrewal <manavgrewal@users.noreply.github.com>; remote origin https://github.com/manavgrewal/BudgetTracker.git; branch main = bc40dd2 (old root) + 29c7797 (docs: spec v1.4 + plan) + 0be2303 (feat: task 1 scaffold) + de127fb (merge GitHub starter LICENSE/README). Pushed, verified. LICENSE = MIT (user chose at repo creation). T19 must keep LICENSE, replace starter README. Commit-per-task from here; push after each task commit is fine (user authorized posting). tsconfig.tsbuildinfo untracked (now gitignored).
- Session interruption note: host process closed mid-T2; T2 agent died pre-write (verified: zero project files). Resumed via SendMessage same agent. Ledger + git history = recovery map, worked as designed.
- Task 2: resumed (implementer sonnet, same agent). BASE now = commit 0be2303/de127fb tree (fresh tree snapshot to be taken at review time). Review model: opus (load-bearing schema).
- Task 2: review (opus) = Needs fixes. 3 Important → fix round 1 dispatched (idempotency test no-op'd by cleanup+mkdir dance; db:generate foot-gun w/ missing snapshot + 3 raw-SQL-only objects; 4 unexecuted test-helper exports incl. INSERT..RETURNING). Trees de127fb..f572c12, package review-de127fb..f572c12.diff.
- Task 2: minors (deferred, 11): schema.ts↔DDL drift unguarded (parent_id FK + name_parent_uq missing from mirror); no CHECK constraints on enum columns; depth-2 only in seed; dead createdAt in seedCategories; importMappingSchema double-cast erases type guarantee; settings.ts+clock.ts untested (deleteSetting undocumented export); BUDGET_DB_PATH/BUDGET_MIGRATIONS_DIR bypass readEnv + undocumented; WAL assertion weak (header-persisted); getIntSetting accepts '12abc'; closeDb lacks double-close guard; TD chequing/visa mappings byte-identical uncommented.
- Task 2: fix round 1/5 (3 addressed, 0 open — idempotency test real now; db:generate removed + dual warnings; 4 helpers smoke-tested; trees f572c12..5d12312)
- Task 2: complete (commit 2094f8e, pushed; review clean after fix round 1). 39 tests green. Stale db:generate mention remains in plan doc (docs-only, inert — script no longer exists).
- Task 3: dispatched (implementer sonnet). BASE commit 2094f8e.
- Task 3: complete (commit 16ad9ae, pushed; review clean, zero fix rounds). 68 tests green. Minors (deferred, 3): safeTz() swallows all readEnv errors (brief-verbatim); safeTz fallback branch untested; date regexes accept mixed separators (harmless). Brief typo confirmed: "12 tests" vs actual 15.
- Task 4: dispatched (implementer sonnet). BASE commit 16ad9ae. Review model: opus (auth core).
- Task 4: review (opus) = Needs fixes, 4 Important (3 brief-mandated). Rulings: (R1) x-url-protocol phantom header removed — scheme from request URL, spec §6 mechanism satisfied properly; cost if wrong: none, plan header was fabricated. (R2) x-forwarded-host gated behind TRUST_PROXY — consistency + defense-in-depth. (R3) CSP: nonce-based attempt with explicit fallback authority to documented unsafe-inline (LAN app, React escaping, no dangerouslySetInnerHTML — acceptable residual risk if fallback taken); cost if wrong: weaker XSS mitigation, hardening path documented. (R4) /api/* exempt from login redirect (handlers own 401s), middleware tests added. Fix round 1 dispatched to original implementer. Trees 16ad9ae..949519e.
- Task 4: minors (deferred, 7): Sec-Fetch-Site:none allows mutating request (brief-mandated, tested); nowIso/toISOString mixing risks lexicographic purge drift; countActiveSessions unused (reviewer recommends delete — declared interface omits it); clearSessionCookie omits secure flag (cosmetic); no headers on _next/static; Origin:null falls to fetch-metadata branch; scheme not compared in origin check.
- Task 4: fix round 1/5 (4 addressed, 0 open — phantom header gone repo-wide, forwarded-host gated, nonce CSP per-request CSPRNG + tested, /api exempt + 8 middleware tests; trees 949519e..c38dab0). Implementer's Playwright-verification claim unevidenced in report — code-level evidence sufficient, noted.
- Task 4: complete (commit e69eb8d, pushed; review clean after fix round 1). 112 tests green. Note: nonce CSP forces whole-app dynamic rendering (layout awaits headers()) — intended consequence, fine for LAN app.
- Task 7 CARRY-FORWARD: login/setup route handlers MUST pass new URL(request.url).protocol as shouldUseSecureCookie's first arg — no production caller exists yet (signature verified, unexercised).
- Task 5: dispatched (implementer sonnet). BASE commit e69eb8d.
- Task 5: review (opus) = Needs fixes. Deviation ADJUDICATED IN IMPLEMENTER'S FAVOR — brief's layer-A algorithm confirmed broken (worked timelines; lockout could end ~14 min early). Rulings: (R5) layer A window-scan fix (monotonic lockout) + normalize to strict < boundary (retryAfterMs:0 incoherence); cost if wrong: overlong lockouts for family members — bounded at 15 min, acceptable. (R6) layer B's mandated algorithm shares the self-release hole — fixed via binding CALLER CONTRACT (no recordLoginAttempt while locked) instead of deviating from spec algorithm; cost if wrong: nothing — contract matches natural login flow. (R7) Minor "success clears other-IP layer-A lockouts" ACCEPTED as spec property (family app; owner login granting fresh budget = fine). Fix round 1 dispatched. Trees e69eb8d..b1a5db8.
- Task 5: minors (deferred): XFF value unvalidated/uncapped when TRUST_PROXY on (layer B mitigates); nowIso redundant ??; boundary tests ±1min not ±1ms.
- Task 6: review (opus) = approved on crypto exactness, 2 Important hygiene findings (brief-inherited: otplib singleton mutation, production Date.now monkeypatch). Ruling (R8): fix both via authenticator.clone (window+epoch) — process requires Important findings fixed or parked; chose fix (trivial, prevents silent replay-window widening + concurrent-time hazard). Cost if wrong: none, storage format unchanged. Fix round 1 dispatched. Trees ed8e320..79285c3. Minors deferred: double-normalize in hashRecoveryCode; decrypt-throw on login path = 500 (T7 must handle deliberately); non-constant-time comparisons (upstream, recorded only).
- Task 6: fix round 1/5 (2 addressed, 0 open; trees 79285c3..1c4472a). Re-reviewer's residual assumption (otplib clone merge semantics) closed by controller: ±1-window tests exercise the epoch-clone path at offset timestamps and pass 25/25 post-fix — merge behavior behaviorally proven.
- Task 6: complete (commit 1b89c62, pushed; review clean after fix round 1). 157 tests green.
- Task 7: dispatched (implementer sonnet). BASE commit 1b89c62. Review model: opus (auth routes + first UI).
- Task 7 CARRY-FORWARD (accumulating): (d) TOTP enrollment route holds candidate secret out-of-band (session/signed cookie), calls enableTotpForUser ONLY after passing verifyTotp — module has no pending-enrollment storage by design; (e) handle decryptTotpSecret throw on login path as clean auth failure, not 500; (a) pass new URL(request.url).protocol to shouldUseSecureCookie; (b) CALLER CONTRACT: check lockout BEFORE verify, never recordLoginAttempt while locked, re-check lockout after argon2 verification (concurrent-overshoot note); (c) Retry-After from retryAfterMs.
- Task 5: fix round 1/5 (3 addressed, 0 open — window-scan monotonic lockout w/ hand-traced timeline proof, bounded floor, 4 regression tests; trees b1a5db8..9e73103)
- Task 5: complete (commit ed8e320, pushed; review clean after fix round 1). 132 tests green.
- Task 6: dispatched (implementer sonnet). BASE commit ed8e320. Review model: opus (TOTP crypto).
- Task 7: review (opus) = Needs fixes, 6 Important. Rulings: (R9) Secure-flag mechanism = unconditional 'http:' + TRUST_PROXY/x-forwarded-proto as sole HTTPS signal (reviewer's alternative adopted; implementer's Origin-scheme derivation violated callee's documented contract); cost if wrong: direct-HTTPS-no-proxy loses Secure flag — rare deployment, documented. (R10) dummy-hash timing-oracle fix mandated (ruling c enforcement). (R11) setup TOCTOU transactional fix + assertSameOrigin in all Server Actions + TOTP-step needsTotp preservation + 4 ruling-regression tests. Fix round 1 dispatched. Trees 1b89c62..2ce715b. Implementer's beyond-brief fixes (middleware / dispatch, recovery-code prop-flip race) both verified correct.
- Task 7: minors (deferred, 10): admin-table ?? masking stale errors; disableTotp without password re-auth (brief-mandated); unused remainingRecoveryCodes RPC export (delete-worthy); dead buildSessionCookieHeader; resetMfa leaves sessions alive (unlike resetPassword — inconsistency); findUserByUsername pulls secrets into render path for one boolean; logout doesn't clear bt_pending_totp; pending-enrollment TTL client-enforced only; // path middleware nit; SPEC GAP: temp passwords don't force change on first login (no mustChangePassword column) — surface to user in final summary.
- Task 7: fix round 1/5 (6 addressed, 0 open; oracle-check on needsTotp generalization passed — pure prior-state echo; trees 2ce715b..dd935ec)
- Task 7: complete (commit 6754722, pushed; review clean after fix round 1). 201 tests green. PHASE 1 COMPLETE.
- Task 8: dispatched (implementer sonnet). BASE commit 6754722. Phase 2 begins.
- Task 8: complete (commit ede0ceb, pushed; review clean, zero fix rounds). 243 tests green. Minors deferred: 2 dead defensive code spots; auto-encoding heuristic blind spot (valid-UTF-8-shaped 1252 bytes) acknowledged as spec property.
- Task 9: dispatched (implementer sonnet). BASE commit ede0ceb. Review model: opus (dedup correctness-critical).
- Task 9: review (opus) = approved on hash correctness (independent digest verification; collision adjudicated unreachable — parseDateString forbids pipes, occurrence key self-heals same-file). 2 Important → fix round 1 (vacuous error-row test; hashVersion plumbing) + folded reviewer-derived golden hex. Trees ede0ceb..f395ca4. Minors deferred: import-graph independence test instead of source-grep; pipe-free-rawDate precondition doc; caller MUST-NOT-reorder contract; \s Unicode-version note; stale CHUNK comment.
- Task 9: fix round 1/5 (3 addressed, 0 open; hash computation verified byte-unchanged; golden hex independently recomputed; trees f395ca4..fb186db)
- Task 9: complete (commit 04e0805, pushed; review clean after fix round 1). 265 tests green.
- Task 10: dispatched (implementer sonnet). BASE commit 04e0805. Review model: opus (undo correctness-critical).
- Task 10 CARRY-FORWARD: insert path must write hashVersion explicitly from HashedRow (never lean on schema DEFAULT 1); dedupHash callers must not reorder rows before hashing.
- Task 10: review (opus) = Needs fixes (1 Important, plan-inherited). Rulings: (R12) implementer's TWO brief-bug claims both ADJUDICATED CONFIRMED — (i) non-numeric amountCents silently stored by SQLite affinity (brief's atomicity test could never throw), (ii) brief's correlated subquery decorrelated by drizzle's projection qualifier-stripping → undo permanently no-op (reviewer EXECUTED both SQL forms; replacement traced correct 14/17 split). Fixes accepted. (R13) provenance-wiping importId-null line + paired dead cascade-covered delete line: both removed, FK onDelete:'set null' owns the behavior; regression test required. Fix round 1 dispatched. Trees 04e0805..180f41c. Minors deferred: partition read outside txn (single-process benign); assertInsertable no Number.isInteger + post-duplicate-branch placement asymmetry.
- Task 10: fix round 1/5 (2 addressed, 0 open; sole-association logic byte-unchanged; trees 180f41c..3e8305f)
- Task 10: complete (commit 9a3f12a, pushed; review clean after fix round 1). 283 tests green.
- Task 11: dispatched (implementer sonnet). BASE commit 9a3f12a.
- Task 11: complete (commit dfa171b, pushed; review clean, zero fix rounds). 319 tests green. Ruling (R14): brief-bug #5 confirmed (star-guard defeated AMZN case; + second unclaimed STORE-token bug silently fixed); STORE-word-always-stripped accepted as v1 spec behavior (normalizer evolvable; parked with ruling). Minors deferred: multi-word city tails leave one word; bare province-word endings over-strip (both inherited spec limitations).
- Task 12: dispatched (implementer sonnet). BASE commit dfa171b. Review model: opus (engine correctness-critical).
- REAL-CSV VALIDATION (user provided TD chequing/TD Visa/Amex exports, gitignored real-statements/): TD Visa preset CORRECT; TD Chequing preset WRONG (quoted-all, LF, YYYY-MM-DD dates); Amex preset WRONG (17 cols, Description@2, Amount@5, DD Mon YYYY, no Category col, multiline in Address fields not Extended Details). Date parsers already compatible — preset values only. Ruling (R15): fix dispatched to T8 agent (presets + fixtures scrubbed-to-real-structure + coupled tests + spec §3 table); Scotia stays best-effort (no sample). Report: real-csv-validation.md. Cost if wrong: none — preview wizard still self-corrects.
- Task 12: review (opus) in flight. Trees dfa171b..4b1e184 (fixture hunks scoped out).
- Task 12: review (opus) = Needs fixes (1 Critical, 3 Important). Rulings: (R16) brief bug CONFIRMED + brief's two tests proven mutually contradictory; implementer's -Infinity fix REJECTED (margin=Infinity disables gate + poisons confidence column); adopted reviewer's Laplace-smoothed prior (numerically verified vs all existing assertions) + finite guard. (R17) new rule_kind 'not_transfer' (exact-only) so manual un-flag survives reruns — no migration needed, TEXT column; excluded from packs like 'rename'. (R18) engine single-transaction; chunked inArray at 400. (R19) folded distinct-token gate fix (PIZZA PIZZA instance-counting hole). Fix round 1 dispatched. Trees dfa171b..4b1e184. Minors deferred: untrain not total (unreachable today); clearCategory deletes shared rule + confirmCategory same-cat early-return won't recreate; applyCategoryToMatching skips rule-source rows at target (never promoted to training set); unused userId params (no audit trail); rename edits rewrite whole table; gitignore real-statements/ hunk = CONTROLLER's own edit (provenance resolved, not fixture agent).
- Task 12: fix round 1/5 (5 addressed, 0 open; smoothed prior hand-recomputed by re-reviewer ≈2.785 matches; trees 4b1e184..c5305dd)
- Task 12: complete + preset fix complete (combined commit d012a24 — snapshot staging merged both streams into one commit, cosmetic only; pushed). 386 tests green. Preset review approved, leak check PASS.
- Task 13: dispatched (implementer sonnet). BASE commit d012a24.
- Task 13: review (opus) = Needs fixes, 6 Important (4 brief-inherited). Ruling (R20): fix all in-task (pre-buffer size checks; engineFailed flag surfaced; parse-before-fork ordering; validate-before-stage; honest undo counts + !ok handling; 404 unknown importId). Fix round 1 dispatched. Trees d012a24..f8d60fd. Minors deferred: unguarded JSON.parse in wizard action; zero-accounts empty state 400s; profile persists across account switch (wrong-bank hazard); preview.skipped never displayed; no busy guard on undo/wizard (double-submit); stagingId unbound to user/account (benign today, T22-era hardening note).
- Task 13: fix round 1/5 (6 addressed, 0 open; trees f8d60fd..c23a29b). Complete (commit 0e25a36, pushed). 435 tests green.
- Task 14: dispatched (implementer sonnet). BASE commit 0e25a36.
- Task 14: review (sonnet) = Needs fixes, 2 Important (archived-category silent-clear/untrain hazard; 5 actions missing zod — 2 uncaught throws, 1 NaN write). Fix round 1 dispatched. Trees 0e25a36..3544371. Minors deferred: REVIEW_WHERE predicate duplicated in listReviewQueue (export for single-source); LIKE %/_ unescaped (imprecise matching only).
- Task 14: fix round 1/5 (2 addressed, 0 open; trees 3544371..4bba220; note: bulk-picker archived-exclusion test coverage overclaimed — code correct, test only mounts 2 of 3 pickers). Complete (commit 25726b2, pushed). 482 tests green. PHASE 2 COMPLETE.
- Task 15: dispatched (implementer sonnet). BASE commit 25726b2. Review model: opus (budget math).
- Task 15: review (opus) = Needs fixes, 6 Important (math verified correct; findings = presentation/edge hazards). Rulings: (R21) headline totals = 3 numbers (budgetedLimit/budgetedSpent/totalSpent — no 320%-over illusion); (R22) archived spend rolls into parents + read-only archived top-level rows; (R23) categorySpend scope implemented (T17 dashboard dependency); (R24) personal+null throws; (R25) zero-limit = real budget (over when spent>0); income rows excluded (existing ruling enforcement); action security tests mandated. Fix round 1 dispatched. Trees 25726b2..c6d0304. Minors deferred: non-admin sees always-fail affordances on others' sections; dual-banner stale states; aria-valuenow range escape; stale inputs after copy; tampered userId → FK throw (admin-only); no depth CHECK constraint (hand-inserted grandchild silently dropped — note for final review); N+1 resolveBudget (fine on SQLite); copy not transactional; dead select field. Admin-override-on-others'-personal-budgets divergence from spec §6 text ACCEPTED (matches brief + family-trust intent) — ruling.
- Task 15: fix round 1/5 (7 addressed, 0 open; trees c6d0304..5234214). Complete (commit ede3916, pushed). 532 tests green.
- Task 16: dispatched (implementer sonnet). BASE commit ede3916.
- Task 16: complete (commit 3572504, pushed; review clean, zero fix rounds — reviewer hand-traced all 17 pace assertions). 571 tests green. Minors deferred: no un-archive UI affordance; dead ?? fallback in GoalCard; no goals-client render test (pattern gap).
- Task 17: dispatched (implementer sonnet). BASE commit 3572504.
- T17 CARRY-FORWARD: budgetTotals now returns {budgetedLimitCents, budgetedSpentCents, totalSpentCents}; BudgetRow gained isArchived/overBudget; categorySpend(month, {scope, attributedUserId}) implemented — dashboard must consume the NEW shapes.
- Task 17: complete (commit fda3d80, pushed; review clean, zero fix rounds). 596 tests green. PHASE 3 COMPLETE. Minors deferred: person-split reconciliation untested (holds by linearity); CSV formula-injection neutralization absent (RFC-only per ruling — flag to final review: imported bank text re-exported to Excel); person filter doesn't scope the split section (interface by design).
- Task 18: dispatched (implementer sonnet). BASE commit fda3d80.
- Task 18: complete (commit 32e6812, pushed; review clean, zero fix rounds — reviewer independently rebuilt + inspected Edge bundle). 618 tests green. Minors deferred: instrumentation-split rationale in report inaccurate (code correct, mental-model nit); no scheduler idempotence test; readFileSync download (LAN-fine).
- Task 19: dispatched (implementer sonnet). BASE commit 32e6812.
- Task 19: review (sonnet) = approved w/ 2 small Importants (HOSTNAME test assertion missing; report overstated env-var verification). Fix round 1 dispatched (+ folded .dockerignore .env exclusion). Trees 32e6812..ffbb9cd. T21 CARRY-FORWARD: scripts/ COPY into runner belongs to Task 21 (reviewer traced — reset-admin-password + its Dockerfile-COPY test live there; final review must confirm it lands). Minor deferred: compose hardcodes PORT 3000 (README doesn't advertise tunable).
- Task 19: fix round 1/5 (3 addressed; trees ffbb9cd..7e25407). Complete (commit 1c444c4, pushed). 638 tests green. PHASE 4 COMPLETE.
- Task 20: dispatched (implementer sonnet). BASE commit 1c444c4.
- Task 20: review (sonnet) = Needs fixes, 1 Important (pack import routes lack pre-buffer size cap; implementer's no-precedent claim factually wrong). Privacy invariant VERIFIED field-by-field (no transactions/amounts/accounts/users/bayes reachable under any flags; not_transfer + rename excluded from export — watch-item closed). Skip-gracefully import verified (watch-item closed). Fix round 1 dispatched. Trees 1c444c4..ed38d0b. Minors deferred: assertNoDeepNesting name-keyed false-positive edge (same name at two levels); two-statement upsert redundancy.
- Task 20: fix round 1/5 (1 addressed; trees ed38d0b..8511e82). Complete (commit 0c9370e, pushed). 698 tests green.
- Task 21: dispatched (implementer sonnet). BASE commit 0c9370e.
- Task 21: review (opus) = Needs fixes, 1 Critical (updaters hardcode :3000 health URL → ported installs auto-rollback healthy updates) + 6 Important (Synology CWD anchoring + --root / hazard; rollback assumes tag exists; rollback leaves git/npm mutations; installers exit 0 on failed health; drift-pin test uses copied literals; stale port override). Rulings: (R26) health via docker inspect container status (port-independent); (R27) rollback-mutation warning documented (code snapshot optional); (R28) failed install health = exit 1, container left for diagnosis. Fix round 1 dispatched w/ 3 folds (false Data-deleted messages, stale compose comment, openssl fallback). Trees 0c9370e..61b855d. Minors deferred: fileMustExist on rescue DB open; buildx multi-arch doc caveat; PS5.1 BOM; health-wait wall-time overrun; sed pipefail fragility; --purge-data has no confirmation (brief-mandated — surface to user in final summary).
- Task 21: fix round 1/5 (1C+6I+3 folds all addressed; trees 61b855d..1590f19). Complete (commit f0c6482, pushed). 784 tests green.
- Task 22: dispatched (implementer sonnet). BASE commit f0c6482. LAST TASK before final review.
- Task 22: review (opus) = Needs fixes. Suspected Critical REFUTED (NULL reaches dedup_hash correctly; implementer's deviation adjudicated correct). Credential audit CLEAN (no path from access URL to logs/responses/errors). 6 Important → fix round 1 (forget-connection lockout; malformed-posted crash post-budget-burn; accounts GET origin check; POST pre-buffer caps; externalId ''-vs-NULL latent; vacuous-and-would-fail rg test) + folded amount-precision divergence. Trees f0c6482..528592c. Minors deferred: N-empty-string dedup prefetch waste; 2N isSimplefinManaged queries on import page; empty imports-row accumulation per no-activity sync (ruling-mandated, history noise); forget-reclaim resets daily counter (needs fresh bridge token, not practical bypass).
- Task 22: fix rounds 1-2 (6+1 fold round 1; tsx-scope round 2; trees f0c6482..fc16fbc). Complete (commit 9c5886e, pushed). 864 tests green. ALL 22 TASKS COMPLETE.
- FINAL whole-branch review dispatched (opus): 6-pass repo review + ledger minors triage. Range ff25a8f..9c5886e (25 commits).
- FINAL REVIEW (opus, 6 passes + independent full-suite run 864/864): verdict NOT READY — 2 Critical: (C1) NO UI CAN CREATE A BANK ACCOUNT — lib fully tested, zero UI callers, no task owned the surface; import pipeline unreachable on fresh install, joint accounts impossible (plan defect, cross-task seam); (C2) documented SECRET_KEY-loss recovery false — rescue script never clears TOTP → permanent lockout. + I1 Synology chown gap, I2 two isSameOrigin omissions, I3 must-change-password spec gap, I4 profile-persists-on-account-switch, I5 zero-account 400. Ledger triage: 94 minors — 89 fine-deferred, 2 escalated (I4/I5), 2 resolved, 1 spec decision (I3).
- Ruling (R29): I3 must_change_password RULED as documented spec deviation (temp passwords advisory under family-trust; admins see everything by design; owner decides post-handoff). Cost if wrong: family member keeps admin-known password — mitigated by them changing it voluntarily in settings.
- Ruling (R30): final fix wave = ONE opus implementer: C1 (accounts manager + wizard step + empty states + action-surface integration test), C2 (--clear-mfa + docs), I1, I2, I4/I5, folded minors m1-m5 (export origin check, sync engine catch, errlist contradiction, cross-origin FAQ, Container Manager terminal doc). Formula-injection/LIKE-escape/archived-copy/manual-txn-engine-skip stay deferred.
- Ruling (R31): download-GET origin semantics — reject only on PRESENT-and-mismatched Origin/Sec-Fetch-Site; both-absent = allow (auth still required). Fix-wave concern 1 confirmed: strict checks 403'd same-origin link-click downloads on plain-HTTP LAN (documented default); latent for backup/download since T18 (tests set headers explicitly). Read-only + auth-gated + CORS-protected = header-absence rejection buys nothing on plain HTTP. Applied to 5 download GETs via shared helper; mutating surfaces stay strict. Cost if wrong: cross-site link can trigger a download to the USER'S OWN machine on plain HTTP — accepted, response unreadable cross-origin.
- Ruling (R32): fix-wave concerns 2 (institution optional lib-wide) and 3 (owner change doesn't rewrite existing attribution — spec-consistent) ACCEPTED; surface #3 to owner in summary.
- CARRY-FORWARD dispatch pointers: T21 Dockerfile MUST COPY drizzle/ into standalone runtime image (boot runs migrate; reviewer-verified .dockerignore doesn't exclude it, but standalone output won't trace it); T14 (category managers) must enforce max-depth-2 in application code; T19/T21 document BUDGET_DB_PATH + BUDGET_MIGRATIONS_DIR in .env.example/README.
- MANDATE (user, 2026-08-16, verbatim intent): "dont stop if you need my input take a best decision on my part and give me a summary in end where you took decisions" — full autonomy through all 22 tasks + final review; ruling protocol per SDD skill; final message must collect ALL Ruling: lines in order with cost-if-wrong. Push per task authorized. Only hard stops: destructive/irreversible ops outside plan scope, security-sensitive beyond spec, plan so broken every path is a guess.

## Post-release round (2026-08-16, owner awake)
- Owner decisions: forced password change IMPLEMENTED (supersedes R29; migration #2, interstitial, admin-create/reset set flag); --purge-data no-confirm ACCEPTED; owner-change-no-attribution-rewrite ACCEPTED; deferred-minors batch APPROVED.
- Polish batch: 14 of 89 deferred minors implemented (formula-injection guard w/ numeric exemption, LIKE escaping, archived budget copy, manual-txn transfer detection, read-only budget sections, goal restore, banner fix, busy guards incl. React-19 useFormStatus correction, skipped count, fileMustExist, REVIEW_WHERE single-source, resetMfa session kill, dead-code sweep, comment fixes). Remaining 75 stay deferred (cosmetic/adjudicated-acceptable).
- Versioning: package.json 1.0.0 source of truth, CHANGELOG.md + Settings→About + footer + health version field; build-time inlined (standalone-cwd trap avoided, verified in built output).
- INSTALL.md: NAS-backup section (SQLite-on-network-FS = data-loss caveat; backups-dir-on-NAS or rsync patterns).
- Stray .vscode/.altemplates files: user's IDE extension, staged by controller snapshots — gitignored, never committed.
- All reviewed (2 scoped reviews, both approved). Final: 79 files / 1006 tests green, typecheck + build clean.
